import type { NextPage, GetServerSideProps } from 'next';
import Head from 'next/head';
import styles from '@/styles/Home.module.css';

import supabase from '@/utils/supabase';
import signInUser from '@/client/utils/auth/signInUser';
import useExampleQuery from '@/client/components/useExampleQuery';
import PageHead from '@/client/components/PageHead';

const Home: NextPage = () => {
  useExampleQuery();

  return (
    <div className={styles.container}>
      <PageHead
        title="Hello world"
        description="Generated by Create Next App"
      />

      <main className={styles.main}>
        <h1 className={styles.title}>
          Welcome to <a href="https://nextjs.org">Next.js!</a>
        </h1>

        <div>
          <button
            onClick={async () => {
              await signInUser({
                email: 'jalebi.labs+a@gmail.com',
                password: 'password',
              });
            }}
          >
            Sign in
          </button>

          <button
            onClick={async () => {
              const data = await supabase.auth.signOut();
              console.log(data);
            }}
          >
            Sign out
          </button>
        </div>

        <div style={{ width: '400px' }}>
          <pre>
            {JSON.stringify(
              { session: supabase.auth.session(), user: supabase.auth.user() },
              null,
              2
            )}
          </pre>
        </div>
      </main>
    </div>
  );
};

export const getServerSideProps: GetServerSideProps = async (context) => {
  const x = await supabase.auth.api.getUserByCookie(context.req);
  const y = supabase;
  return {
    props: {
      // x,
    },
  };
};

export default Home;
